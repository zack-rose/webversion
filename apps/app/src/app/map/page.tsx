import { MapSectionContainer } from "@/features/map/components/map.client";

const MapScreen = () => {
	return (
		<main className="content-spacing bg-3deg-cream-200 pb-[5%]">
			<MapSectionContainer />
		</main>
	);
};

export default MapScreen;
